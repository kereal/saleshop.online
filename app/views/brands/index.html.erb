<div class="brands clearfix">

  <h1>Все бренды</h1>

  <% @brands.each do |brand| %>

    <div class="brand" style="width:25%;float:left;">
      <%= link_to image_tag(brand.image(:medium), alt: brand.title, style: "width:100%"), brand %>
    </div>

  <% end %>

</div>